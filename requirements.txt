# VulnHunter v3.0 — Runtime dependencies
requests>=2.31.0
urllib3>=2.0.0

# Optional — required for --profile / YAML support
pyyaml>=6.0.1

# Development / testing
# pip install -e ".[dev]"   to get pytest + pytest-cov automatically
